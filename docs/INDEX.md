# Docs

Durable reference for the Arch bootstrap and Chezmoi-managed setup.

## References

- **chezmoi-setup.md**: Catalog of the current Chezmoi source tree, setup scripts, managed config, and remaining role-migration work.

## Notes

- This repo is the central AI working repo and knowledge base for bootstrap and Chezmoi setup decisions.
- Chezmoi source currently lives at `~/.local/share/chezmoi`.
- The root `bootstrap.sh` is the only long-term setup script owned by this repo.
- Recent repo-local context lives in root `scratchpad.md`.
